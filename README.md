# Desafio Lyncas
*App para consulta de livros utilizando api google books*

**Para iniciar o server**

 - `cd server`
 - `npm install`
 - `npm start`
 *localhost:3000*

**Para iniciar o client**

- `cd client`
- `npm install`
- `npm start`
*localhost:4200*